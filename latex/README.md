# Dissertation LaTeX

Final submitted paper source. Entry point: [main.tex](main.tex). From this folder use `latexmk -pdf main.tex` with a full LaTeX/BibTeX installation. The exact submitted [PDF](../Dissertation.pdf) is kept at the repository root. Do not treat a recompilation date or PDF identifier as a new submission.
