# GRES (GPU) definitions for the simulated Stanage cluster.
# No File= devices are listed because the simulator has no real GPUs;
# only the counts/types matter for scheduling.
NodeName=gpua100[01-13]   Name=gpu Type=a100    Count=4
NodeName=gpuh100[01-06]   Name=gpu Type=h100    Count=2
NodeName=gpuh100nvl[01-04] Name=gpu Type=h100nvl Count=4
